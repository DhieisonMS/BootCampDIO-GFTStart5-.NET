pipeline {
    agent { label 'windows' }
    stages {
        
        stage('Test on Windows') {
            steps {
                script:
                    dir('C:\\') {}
                    echo "teste"
            }
        }
    
    }
}