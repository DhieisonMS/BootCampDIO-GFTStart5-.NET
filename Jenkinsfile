pipeline {
    agent { label 'windows' }
    stages {
        
        stage('Test on Windows') {
            steps {
                dir('C:\\') {
                    bat "mkdir teste"               
                }
            }
        }
    
    }
}